#include<stdio.h> 
 int main(int argc, char *argv[])
{
	float principal, time, interest, total;
	printf("Enter principal (100 Rs to 1,000,000 Rs): ");    scanf("%f",&principal);
    if (principal >= 100 && principal <= 1000000){	
    	printf("Enter interest rate (5  to 10): ");    scanf("%f",&interest);
    	if(interest >= 5 && interest <= 10){
    		printf("Enter time period (1 to 10 years): ");    scanf("%f",&time);
    	    if(time >= 1 && time<= 10){
   	total = principal * (interest/100) * time;
    	printf("total sinple interest is: %.2f", total);}			}
    }
	}